{
  "name": "rkeeper-agent",
  "version": "1.0.0",
  "description": "R-Keeper Report Agent - standalone .exe that syncs SQL data to cloud",
  "main": "agent.js",
  "scripts": {
    "start": "node agent.js",
    "test": "node agent.js --test",
    "once": "node agent.js --once",
    "setup": "node agent.js --setup",
    "build": "node build.js"
  },
  "dependencies": {
    "mssql": "^10.0.4"
  },
  "devDependencies": {
    "@yao-pkg/pkg": "^6.20.0",
    "esbuild": "^0.27.0",
    "postject": "^1.1.0"
  }
}
